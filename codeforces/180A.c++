/*
Codeforces 180A - Defragmentation
Source: https://codeforces.com/problemset/problem/180/A

Problem statement (paraphrased):
A disk has n clusters and several fragmented files; each file's listed cluster
order is the order of its contents, and at least one cluster is free. Output no
more than 2n copy operations that rearrange the files into contiguous blocks
from the start of the disk, preserving each file's fragment order and leaving
all unused clusters at the end. The files themselves may appear in any order.
*/
